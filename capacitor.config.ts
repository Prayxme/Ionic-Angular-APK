import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.cadif1',
  appName: 'desafio1 apk',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
